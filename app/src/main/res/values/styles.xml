<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="PrimaryText" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">#00FF00</item>
        <item name="android:textSize">20dp</item>
        <item name="android:typeface">monospace</item>
    </style>

    <style name="Button1" parent="StandartMargins">
        <item name="app:layout_constraintEnd_toEndOf">parent</item>
        <item name="app:layout_constraintTop_toTopOf">parent</item>
    </style>

    <style name="StandartMargins" parent="PrimaryText">
        <item name="android:layout_margin">8dp</item>
    </style>
</resources>
